[package]
name = "south-common"
version = "3.0.0"
edition = "2024"

[features]
default = [ ]

g0 = [ "embedded", "embassy-stm32/stm32g0b1ke" ]
h7 = [ "embedded", "embassy-stm32/stm32h723vg" ]

embedded = [ "embassy-stm32", "defmt", "heapless", "embedded-can" ]
ground = ["tmtc-system/ground", "dep:serde", "dep:libm" ]

[dependencies]
tmtc-system = { version = "4.6.0", git = "https://github.com/S2outh/telemetry-system.git" }

# config
lsm6dsv32 = { git = "ssh://git@github.com/S2outh/imu-driver.git", default-features = false }
spin = { version = "0.10.0", features = ["lazy"], default-features = false }

# air
embassy-stm32 = { version = "0.5.0", features = [ "defmt" ], optional = true }
defmt = { version = "1.0", optional = true }
heapless = { version = "0.9", default-features = false, optional = true }
embedded-can = { version = "0.4.1", optional = true }

# ground
libm = { version = "0.2.16", optional = true }
serde = { version = "1.0.228", default-features = false, features = ["derive"], optional = true }
bitflags = { version = "2.11.0", features = ["serde"] }
